import { DefaultTheme } from "react-native-paper";

export const theme = {
    ...DefaultTheme,
    colors: {
        ...DefaultTheme.colors,
        primary:"rgb(0,121,194)",
        background:"transpoarent"
    },
    cardButton: {
        margin:2,
        marginLeft:0,
        marginRight:0
    }

}